const $=s=>document.querySelector(s);const app=$('#app');let token=localStorage.getItem('infra_token');let user=null;let state={view:'dashboard',data:null,lang:localStorage.getItem('infra_lang')||'en'};
const icon={dashboard:'◉',projects:'▦',schedule:'☷',reports:'▤',ai:'✦',analytics:'◔',alerts:'⚠',files:'↗',users:'♟',settings:'⚙'};
function esc(s){return String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}function pct(v){return Math.max(0,Math.min(100,Number(v)||0))}function toast(msg){const d=document.createElement('div');d.className='toast';d.textContent=msg;document.body.appendChild(d);setTimeout(()=>d.remove(),2800)}async function api(url,opt={}){opt.headers=opt.headers||{};if(token)opt.headers.Authorization='Bearer '+token;if(opt.body&&!(opt.body instanceof FormData))opt.headers['Content-Type']='application/json';const r=await fetch(url,opt);let data={};try{data=await r.json()}catch{}if(!r.ok)throw new Error(data.error||'Request failed');return data}
function authView(mode='landing'){
  const isLanding=mode==='landing';
  app.innerHTML=`<div class="${isLanding?'landing-shell':'auth-shell'}">
    ${isLanding?landingPage():(`<section class="auth-hero">
      <div class="brand"><div class="brand-mark">◎</div><div><div class="brand-title">Infra-Pulse</div><div class="brand-sub">PROJECT INTELLIGENCE</div></div></div>
      <div class="auth-copy"><h1>${mode==='login'?'Welcome back.':'Build your workspace.'}</h1><p>${mode==='login'?'Securely access project dashboards, schedules, AI matching, reports and alerts.':'Create a professional project account with strong credentials and a protected profile.'}</p><ul><li>Role-based access</li><li>Live progress intelligence</li><li>Document analysis</li><li>Audit-ready reports</li></ul></div>
    </section><section class="auth-card"><div class="form-card"><div class="auth-form-brand"><div class="brand"><div class="brand-mark">◎</div><div><div class="brand-title">Infra-Pulse</div><div class="brand-sub">PROJECT INTELLIGENCE</div></div></div></div>${mode==='login'?loginForm():registerForm()}</div></section>`)}
  </div>`;
  if(!isLanding) bindAuth(mode);
}
function landingPage(){return `<div class="landing-overlay"></div><header class="landing-nav"><div class="brand"><div class="brand-mark">◎</div><div><div class="brand-title">Infra-Pulse</div><div class="brand-sub">INFRASTRUCTURE PROJECT INTELLIGENCE</div></div></div><div class="landing-actions"><button class="landing-link" onclick="authView('login')">Login Now</button><button class="btn primary" onclick="authView('register')">Register Now</button></div></header><main class="landing-main"><div class="landing-left"><span class="landing-pill">SIH 2026 · PS 122</span><h1>Bridge <span>Planning</span> to<br>Execution.</h1><p>Infra-Pulse turns schedules, site reports and project documents into structured progress intelligence. Match activities, track actuals, detect delays and keep every project decision data-driven.</p><div class="landing-cta"><button class="btn primary landing-primary" onclick="authView('login')">Login Now →</button><button class="landing-outline" onclick="authView('register')">Create Account</button></div></div><div class="landing-panel"><h2>Live Project Intelligence</h2><div class="landing-row"><span>Schedule linking</span><b>AI assisted</b></div><div class="landing-row"><span>Progress tracking</span><b>Real-time</b></div><div class="landing-row"><span>Document extraction</span><b>CSV · TXT · PDF</b></div><div class="landing-row"><span>Delay detection</span><b>Automated</b></div><div class="landing-row"><span>Data storage</span><b>API + Database</b></div></div></main><footer class="landing-footer">© 2026 Infra-Pulse · SIH PS 122 · Intelligent Data Capture & Schedule-Linking Layer</footer>`}
function loginForm(){return `<div class="form-card-title"><h2>Login to Infra-Pulse</h2><div class="sub">Use username or registered email.</div></div><form id="authForm"><div class="field"><label>Username / Email</label><input name="identifier" required autocomplete="username"></div><div class="field"><label>Password</label><input type="password" name="password" required autocomplete="current-password"></div><div class="field"><label id="captchaLabel">CAPTCHA · Loading…</label><div class="captcha-row"><input id="captchaAnswer" name="captchaAnswer" required placeholder="Enter answer"><button type="button" class="btn secondary captcha-button" id="captchaQ">↻ New CAPTCHA</button></div><input type="hidden" name="captchaKey" id="captchaKey"></div><button class="btn primary auth-submit">Login Securely</button></form><p class="auth-bottom">New here? <span class="auth-link" id="switchAuth">Create an account</span> · <span class="auth-link" onclick="authView('landing')">Back to home</span></p><div id="authStatus" class="auth-status hidden"></div>`}
function registerForm(){return `<div class="form-card-title"><h2>Create your account</h2><div class="sub">Professional registration for project intelligence.</div></div><form id="authForm"><div class="form-grid"><div class="field"><label>Full Name *</label><input name="full_name" required autocomplete="name"></div><div class="field"><label>Username *</label><input name="username" pattern="[A-Za-z0-9_.-]{3,40}" required autocomplete="username"></div><div class="field"><label>Email *</label><input type="email" name="email" required autocomplete="email"></div><div class="field"><label>Phone</label><input name="phone" autocomplete="tel"></div><div class="field"><label>Company</label><input name="company"></div><div class="field"><label>Department</label><input name="department"></div><div class="field"><label>Password *</label><input type="password" name="password" id="regPassword" required autocomplete="new-password"><div class="password-hint">✓ 8+ characters · ✓ uppercase · ✓ lowercase · ✓ number · ✓ special character</div></div><div class="field"><label>Confirm Password *</label><input type="password" name="confirm_password" id="regConfirm" required autocomplete="new-password"></div></div><div class="field"><label id="captchaLabel">CAPTCHA · Loading…</label><div class="captcha-row"><input name="captchaAnswer" required placeholder="Enter answer"><button type="button" class="btn secondary captcha-button" id="captchaQ">↻ New CAPTCHA</button></div><input type="hidden" name="captchaKey" id="captchaKey"></div><div class="field"><label>Preferred language</label><select name="preferred_language"><option value="en">English</option><option value="hi">हिन्दी</option></select></div><label class="terms"><input type="checkbox" name="terms" required> <span>I agree to the project application terms.</span></label><button class="btn primary auth-submit">Register Securely</button></form><p class="auth-bottom">Already registered? <span class="auth-link" id="switchAuth">Login</span> · <span class="auth-link" onclick="authView('landing')">Back to home</span></p><div id="authStatus" class="auth-status hidden"></div>`}
async function loadCaptcha(){try{const d=await fetch('/api/captcha').then(r=>r.json());const key=$('#captchaKey');const labels=document.querySelectorAll('#captchaLabel');if(key)key.value=d.key;labels.forEach(x=>x.textContent='CAPTCHA · '+d.question)}catch{showAuthStatus('Unable to load CAPTCHA. Check that the backend is running.')}}
function showAuthStatus(message){const el=$('#authStatus');if(el){el.textContent=message;el.classList.remove('hidden')}}
async function bindAuth(mode){
  loadCaptcha();
  $('#captchaQ')?.addEventListener('click',loadCaptcha);
  $('#switchAuth').onclick=()=>authView(mode==='login'?'register':'login');
  $('#authForm').onsubmit=async e=>{
    e.preventDefault();
    const f=new FormData(e.currentTarget);const body=Object.fromEntries(f.entries());
    if(mode==='register' && body.password!==body.confirm_password){showAuthStatus('Passwords do not match.');return}
    delete body.confirm_password;delete body.terms;
    try{
      const d=await api(mode==='login'?'/api/auth/login':'/api/auth/register',{method:'POST',body:JSON.stringify(body)});
      token=d.token;user=d.user;localStorage.setItem('infra_token',token);localStorage.setItem('infra_lang',user.preferred_language||'en');state.lang=user.preferred_language||'en';renderShell();toast(mode==='login'?'Signed in successfully':'Account created successfully');
    }catch(err){showAuthStatus(err.message);loadCaptcha()}
  }
}
async function boot(){if(!token)return authView('landing');try{const d=await api('/api/auth/me');user=d.user;state.lang=user.preferred_language||state.lang;document.body.classList.toggle('dark',user.theme==='dark');renderShell()}catch{token=null;localStorage.removeItem('infra_token');authView('landing')}}
function renderShell(){app.innerHTML=`<div class="shell"><aside class="sidebar" id="sidebar"><div class="brand"><div class="brand-mark">◎</div><div><div class="brand-title">Infra-Pulse</div><div class="brand-sub">PS 122 · PROJECT INTELLIGENCE</div></div></div><div class="profile-mini"><div class="avatar">${esc((user.full_name||'PM').slice(0,2).toUpperCase())}</div><div><strong>${esc(user.full_name)}</strong><span>${esc(user.role||'Project Manager')}</span></div></div><nav class="nav">${navBtn('dashboard','Dashboard')}${navBtn('projects','Projects')}${navBtn('schedule','Schedule')}${navBtn('reports','Daily Reports')}${navBtn('ai','AI Matching')}${navBtn('analytics','Analytics')}${navBtn('alerts','Alerts')}${navBtn('files','Data Files')}${navBtn('users','Users')}${navBtn('settings','Settings')}</nav><div class="sidebar-bottom">● API online · SQLite database<br><span style="display:inline-block;margin-top:7px">Private account workspace</span></div></aside><main class="main"><header class="topbar"><div style="display:flex;align-items:center;gap:12px"><button class="icon-btn mobile-menu" id="hamb">☰</button><div><div class="crumb">SIH 2026 / PS 122 / INFRA-PULSE</div><div class="page-title" id="pageTitle">Project Dashboard</div></div></div><div class="top-actions"><div class="search-wrap"><span class="search-icon">⌕</span><input id="globalSearch" class="search-input" placeholder="Search activities, projects, reports..."><div id="searchResults" class="search-results hidden"></div></div><button class="icon-btn" id="themeBtn">☼</button><button class="profile-btn" id="profileBtn">◉ ${esc(user.full_name||'Profile')}</button></div></header><section class="content" id="content"></section><footer class="footer">© 2026 Infra-Pulse · SIH PS 122 · Intelligent Data Capture & Schedule-Linking Layer · Secure API · SQLite · Analytics</footer></main></div><div id="modalRoot"></div>`;$('#hamb').onclick=()=>$('#sidebar').classList.toggle('open');$('#profileBtn').onclick=()=>openProfile();$('#themeBtn').onclick=toggleTheme;$('#globalSearch').oninput=searchGlobal;document.body.classList.toggle('dark',user.theme==='dark');navigate(state.view);}
function navBtn(k,label){return `<button data-nav="${k}" class="${state.view===k?'active':''}"><span class="icon">${icon[k]}</span>${label}</button>`}
function navigate(v){state.view=v;document.querySelectorAll('[data-nav]').forEach(b=>b.classList.toggle('active',b.dataset.nav===v));$('#sidebar')?.classList.remove('open');const titles={dashboard:'Project Dashboard',projects:'Projects & Portfolio',schedule:'Schedule & Activities',reports:'Daily Progress Reports',ai:'AI Project Copilot',analytics:'Portfolio Analytics',alerts:'Alerts & Exceptions',files:'Document Intelligence',users:'Users & Access',settings:'Settings'};$('#pageTitle').textContent=titles[v]||'Infra-Pulse';const fn={dashboard:dashboard,projects:projects,schedule:schedule,reports:reports,ai:ai,analytics:analytics,alerts:alerts,files:files,users:users,settings:settings}[v];fn();document.querySelectorAll('[data-nav]').forEach(b=>b.onclick=()=>navigate(b.dataset.nav))}
async function dashboard(){const d=await api('/api/dashboard');state.data=d;const s=d.stats;$('#content').innerHTML=`<div class="grid kpis"><div class="card kpi"><div class="label">Total Activities</div><div class="value">${s.total}</div><div class="hint">Across your projects</div></div><div class="card kpi green"><div class="label">Completed</div><div class="value">${s.completed}</div><div class="hint">Activities closed</div></div><div class="card kpi orange"><div class="label">In Progress</div><div class="value">${s.in_progress}</div><div class="hint">Being monitored</div></div><div class="card kpi red"><div class="label">Delayed</div><div class="value">${s.delayed}</div><div class="hint">Need attention</div></div><div class="card kpi purple"><div class="label">Open Alerts</div><div class="value">${s.open_alerts}</div><div class="hint">From database</div></div></div><div class="grid two-col" style="margin-top:18px"><div class="card"><div class="card-head"><h3>Planned vs Actual</h3><span class="tag">Portfolio progress</span></div><div class="card-body chart-row"><div style="text-align:center"><div class="donut" style="--p:${s.actual}%"><div class="donut-value">${Math.round(s.actual)}%</div></div><div style="margin-top:10px;color:var(--muted);font-size:12px">Actual portfolio progress</div></div><div class="bars"><div class="barline"><span>Planned</span><div class="bar"><i style="width:${s.planned}%"></i></div><b>${s.planned}%</b></div><div class="barline"><span>Actual</span><div class="bar green"><i style="width:${s.actual}%"></i></div><b>${s.actual}%</b></div>${d.projects.map(p=>`<div class="barline"><span title="${esc(p.name)}">${esc(p.name).slice(0,20)}</span><div class="bar purple"><i style="width:${pct(p.actual_progress)}%"></i></div><b>${pct(p.actual_progress)}%</b></div>`).join('')}</div></div></div><div class="card"><div class="card-head"><h3>Priority Alerts</h3><button class="btn secondary small" onclick="navigate('alerts')">View all</button></div><div class="card-body">${d.alerts.length?d.alerts.slice(0,4).map(a=>`<div class="alert" style="margin-bottom:10px"><strong>⚠ ${esc(a.title)}</strong><span>${esc(a.message)}</span></div>`).join(''):'<div class="tag">No open alerts from your database.</div>'}</div></div></div><div class="card" style="margin-top:18px"><div class="card-head"><h3>Recent Daily Reports</h3><button class="btn primary small" onclick="openReportForm()">+ Add report</button></div><div class="card-body table-wrap"><table class="table"><thead><tr><th>Date</th><th>Activity</th><th>Description</th><th>Quantity</th><th>Match</th></tr></thead><tbody>${d.reports.map(r=>`<tr><td>${esc(r.report_date)}</td><td><b>${esc(r.activity_code||'—')}</b></td><td>${esc(r.description||'—')}</td><td>${esc(r.quantity??'—')} ${esc(r.unit||'')}</td><td>${r.ai_match_score===null?'—':esc(r.ai_match_score)+'% · DB'}</td></tr>`).join('')||'<tr><td colspan="5">No reports yet.</td></tr>'}</tbody></table></div></div>`}
async function projects(){const d=await api('/api/projects');$('#content').innerHTML=`<div class="section-head"><div><h2>Projects & Portfolio</h2><p>Create, edit and remove projects using database-backed forms.</p></div><button class="btn primary" onclick="openProjectForm()">+ Add Project</button></div><div class="grid project-grid">${d.projects.map(p=>projectCard(p)).join('')||empty('No projects yet','Create your first project or use Settings → Import test data for your own account.')}</div>`}
function projectCard(p){return `<article class="card project-card"><div class="project-top"><div><h3>${esc(p.name)}</h3><div class="meta">${esc(p.client||'—')} · ${esc(p.location||'—')}${p.state?', '+esc(p.state):''}</div></div><span class="status">${esc(p.status)}</span></div><div class="meta" style="margin-top:24px">${esc(p.start_date||'—')} → ${esc(p.end_date||'—')}</div><div class="progress"><i style="width:${pct(p.actual_progress)}%"></i></div><div class="progress-label"><span>Overall progress</span><b>${pct(p.actual_progress)}%</b></div><div class="actions"><button class="btn secondary small" onclick="openProjectForm('${p.id}')">Edit</button><button class="btn secondary small" onclick="viewProjectActivities('${p.id}')">View schedule</button><button class="btn danger small" onclick="removeProject('${p.id}')">Remove</button></div></article>`}
async function schedule(projectId){const d=await api('/api/schedule'+(projectId?'?project_id='+encodeURIComponent(projectId):''));const title=projectId?(d.activities[0]?.project_name||'Selected Project'):'All Project Schedules';$('#content').innerHTML=`<div class="section-head"><div><h2>${esc(title)} · Schedule</h2><p>${projectId?'Only activities belonging to this project are shown.':'Database-backed schedule across your projects.'}</p></div><div style="display:flex;gap:8px"><button class="btn secondary" onclick="navigate('schedule')">All schedules</button><button class="btn primary" onclick="openActivityForm()">+ Add Activity</button></div></div><div class="card table-wrap"><table class="table"><thead><tr><th>Code</th><th>Activity</th><th>Project</th><th>Planned Start</th><th>Planned End</th><th>Planned</th><th>Actual</th><th>Variance</th><th>Status</th><th></th></tr></thead><tbody>${d.activities.map(a=>`<tr><td><b>${esc(a.code)}</b></td><td>${esc(a.name)}</td><td>${esc(a.project_name)}</td><td>${esc(a.planned_start||'—')}</td><td>${esc(a.planned_end||'—')}</td><td>${pct(a.planned_progress)}%</td><td>${pct(a.actual_progress)}%</td><td>${(Number(a.actual_progress)-Number(a.planned_progress)).toFixed(1)} pts</td><td><span class="status">${esc(a.status)}</span></td><td><button class="btn secondary small" onclick='openActivityForm(${JSON.stringify(a)})'>Edit</button> <button class="btn danger small" onclick="removeActivity('${a.id}')">Delete</button></td></tr>`).join('')||'<tr><td colspan="10">No activities for this project.</td></tr>'}</tbody></table></div>`}
async function reports(){const [r,p,d]=await Promise.all([api('/api/reports'),api('/api/projects'),api('/api/documents')]);$('#content').innerHTML=`<div class="section-head"><div><h2>Daily Progress Reports</h2><p>Submit field data with database-linked activity matching and GPS location.</p></div><button class="btn primary" onclick="openReportForm()">+ Add report</button></div><div class="grid page-grid"><div class="card full"><div class="card-head"><h3>Recent reports</h3><span class="tag">${r.reports.length} stored</span></div><div class="card-body table-wrap"><table class="table"><thead><tr><th>Date</th><th>Project</th><th>Activity</th><th>Location</th><th>Description</th><th>Match</th></tr></thead><tbody>${r.reports.map(x=>`<tr><td>${esc(x.report_date)}</td><td>${esc(x.project_name||'—')}</td><td>${esc(x.activity_code||'—')}</td><td>${esc(x.location_text||'—')}</td><td>${esc(x.description||'—')}</td><td>${x.ai_match_score===null?'Not calculated':esc(x.ai_match_score)+'% · database'}</td></tr>`).join('')||'<tr><td colspan="6">No reports yet.</td></tr>'}</tbody></table></div></div><div class="card"><div class="card-head"><h3>Location capture</h3></div><div class="card-body"><p style="color:var(--muted)">The report form includes India city suggestions and browser GPS. Open it to place a pin on the map.</p><button class="btn secondary" onclick="openReportForm()">Open report form</button></div></div><div class="card"><div class="card-head"><h3>Documents linked</h3></div><div class="card-body"><b>${d.documents.length}</b> documents stored. Match evidence can be reviewed from Data Files.</div></div></div>`}
async function ai(){const h=await api('/api/ai/history');$('#content').innerHTML=`<div class="grid two-col"><div class="card chat"><div class="card-head"><div><h3>Infra-Pulse Copilot</h3><span class="tag">Database-grounded · ${state.lang==='hi'?'हिन्दी':'English'}</span></div><div><button class="btn secondary small" id="langToggle">${state.lang==='en'?'हिन्दी':'English'}</button></div></div><div class="chat-body" id="chatBody">${h.messages.map(m=>`<div class="bubble ${m.role==='user'?'user':'assistant'}">${esc(m.content)}</div>`).join('')||`<div class="bubble assistant">${state.lang==='hi'?'नमस्ते। अपने projects, progress, alerts या uploaded documents के बारे में पूछें।':'Hello. Ask me about your projects, progress, alerts or uploaded documents. I only use data stored in your account.'}</div>`}</div><div class="chat-input"><button class="btn secondary" id="micBtn" title="Use microphone">🎙</button><input id="chatInput" placeholder="${state.lang==='hi'?'अपने project के बारे में पूछें...':'Ask about your project data...'}"><button class="btn primary" id="sendChat">Send</button></div></div><div class="card"><div class="card-head"><h3>What the Copilot can answer</h3></div><div class="card-body"><div class="grid" style="gap:10px"><button class="btn secondary" onclick="askAI('Which projects do I have?')">Which projects do I have?</button><button class="btn secondary" onclick="askAI('Which alerts are open?')">Which alerts are open?</button><button class="btn secondary" onclick="askAI('Summarize my progress')">Summarize my progress</button><button class="btn secondary" onclick="askAI('What documents are uploaded?')">What documents are uploaded?</button></div><hr style="border:0;border-top:1px solid var(--line);margin:22px 0"><p style="color:var(--muted);font-size:12px">History is stored in your SQLite database per user. Microphone input uses the browser Speech Recognition API when available.</p></div></div></div>`;$('#langToggle').onclick=()=>{state.lang=state.lang==='en'?'hi':'en';localStorage.setItem('infra_lang',state.lang);ai()};$('#sendChat').onclick=()=>sendAI();$('#chatInput').onkeydown=e=>{if(e.key==='Enter')sendAI()};$('#micBtn').onclick=startSpeech;scrollChat()}
async function sendAI(){const input=$('#chatInput');if(!input.value.trim())return;const text=input.value.trim();input.value='';appendBubble('user',text);try{const d=await api('/api/ai/chat',{method:'POST',body:JSON.stringify({question:text,language:state.lang})});appendBubble('assistant',d.answer);scrollChat()}catch(e){appendBubble('assistant','Error: '+e.message)}}function askAI(t){$('#chatInput').value=t;sendAI()}function appendBubble(role,text){$('#chatBody').insertAdjacentHTML('beforeend',`<div class="bubble ${role}">${esc(text)}</div>`)}function scrollChat(){const b=$('#chatBody');if(b)b.scrollTop=b.scrollHeight}function startSpeech(){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR)return toast('Speech recognition is not supported by this browser.');const r=new SR();r.lang=state.lang==='hi'?'hi-IN':'en-IN';r.onresult=e=>{$('#chatInput').value=e.results[0][0].transcript};r.onerror=()=>toast('Microphone permission or speech recognition failed.');r.start()}
async function analytics(){const [d,risk]=await Promise.all([api('/api/dashboard'),api('/api/risk-radar')]);const projects=d.projects;$('#content').innerHTML=`<div class="grid metric-grid"><div class="card metric"><div class="label">Projects</div><div class="value">${projects.length}</div></div><div class="card metric"><div class="label">Planned</div><div class="value">${d.stats.planned}%</div></div><div class="card metric"><div class="label">Actual</div><div class="value">${d.stats.actual}%</div></div><div class="card metric"><div class="label">Open Alerts</div><div class="value">${d.stats.open_alerts}</div></div></div><div class="grid page-grid" style="margin-top:18px"><div class="card"><div class="card-head"><h3>Project progress</h3></div><div class="card-body bars">${projects.map(p=>`<div class="barline"><span title="${esc(p.name)}">${esc(p.name).slice(0,20)}</span><div class="bar"><i style="width:${pct(p.actual_progress)}%"></i></div><b>${pct(p.actual_progress)}%</b></div>`).join('')||'<span class="tag">No project data yet.</span>'}</div></div><div class="card"><div class="card-head"><h3>Risk Radar</h3><span class="tag">Database-derived</span></div><div class="card-body">${risk.projects.map(x=>`<div style="padding:12px 0;border-bottom:1px solid var(--line)"><b>${esc(x.project.name)}</b><span class="tag" style="margin-left:8px">${esc(x.health)}</span><div style="color:var(--muted);font-size:13px;margin-top:6px">${x.risks.length?x.risks.map(r=>esc(r.title)+': '+esc(r.detail)).join(' · '):'No current risk condition detected.'}</div></div>`).join('')||'<span class="tag">No project data yet.</span>'}</div></div></div>`}
async function alerts(){const d=await api('/api/alerts');$('#content').innerHTML=`<div class="section-head"><div><h2>Alerts & Exceptions</h2><p>Every alert shown here is stored in your database.</p></div></div><div class="grid">${d.alerts.map(a=>`<div class="card" style="padding:20px;display:flex;justify-content:space-between;gap:15px;align-items:center"><div><span class="tag">${esc(a.severity)}</span><h3 style="margin:10px 0 6px">${esc(a.title)}</h3><p style="margin:0;color:var(--muted)">${esc(a.message)}</p><small style="color:var(--muted)">${esc(a.project_name||'')}</small></div><button class="btn ${a.resolved?'secondary':'primary'} small" onclick="resolveAlert('${a.id}',${!a.resolved})">${a.resolved?'Reopen':'Resolve'}</button></div>`).join('')||empty('No alerts','Alerts will appear here when database conditions create them.')}</div>`}
async function files(){const d=await api('/api/documents');const p=await api('/api/projects');$('#content').innerHTML=`<div class="section-head"><div><h2>Document Intelligence</h2><p>Upload a CSV, TXT or PDF. Infra-Pulse extracts facts, links them to projects and lets you approve database changes.</p></div></div><div class="grid page-grid"><div class="card full"><div class="card-body"><div class="file-drop"><h3>Upload & Analyze</h3><p>Reliable offline formats: CSV · TXT · PDF</p><form id="fileForm"><input id="fileInput" type="file" accept=".csv,.txt,.pdf" required><select id="fileProject" style="margin-top:15px;max-width:400px"><option value="">Auto-detect project</option>${p.projects.map(x=>`<option value="${x.id}">${esc(x.name)}</option>`).join('')}</select><div style="margin-top:15px"><button class="btn primary">Extract Project Data</button></div></form></div><div class="file-list">${d.documents.map(x=>`<div class="file-item"><div><b>${esc(x.original_name)}</b><div class="tag">${esc(x.mime_type||'file')} · ${Math.round((x.size_bytes||0)/1024)} KB · ${x.project_id?'linked to project':'project not linked yet'}</div></div><button class="btn secondary small" onclick="reviewDocument('${x.id}')">Review facts</button></div>`).join('')||'<span class="tag">No files uploaded yet.</span>'}</div></div></div><div class="card"><div class="card-head"><h3>Safe apply workflow</h3></div><div class="card-body"><ol style="line-height:2;color:var(--muted)"><li>Read actual file contents</li><li>Extract project/activity facts</li><li>Compare against your database</li><li>Review each fact</li><li>Accept or reject</li><li>Accepted facts update Projects & Schedule</li><li>Risk alerts recalculate from stored data</li></ol></div></div><div class="card"><div class="card-head"><h3>Included test files</h3></div><div class="card-body"><p style="color:var(--muted)"><b>sample-data/site-progress-report.pdf</b><br><b>sample-data/daily-progress.csv</b></p></div></div></div>`;$('#fileForm').onsubmit=async e=>{e.preventDefault();const fd=new FormData();fd.append('file',$('#fileInput').files[0]);if($('#fileProject').value)fd.append('project_id',$('#fileProject').value);try{const x=await api('/api/documents/upload',{method:'POST',body:fd});toast(`${x.analysis.fact_count} facts extracted. Review them before applying.`);files();setTimeout(()=>reviewDocument(x.document.id),150)}catch(e){toast(e.message)}}}
async function reviewDocument(id){try{const d=await api('/api/documents/'+id+'/facts');openModal('Review extracted project data',`<p style="color:var(--muted)">${esc(d.document.original_name)} · ${d.facts.length} extracted facts. Accept only information you want written to the database.</p><div id="factsBox">${d.facts.map(f=>`<label class="card" style="display:block;margin:10px 0;padding:14px"><input type="checkbox" class="factCheck" value="${f.id}" ${f.status==='pending'?'checked disabled':''} data-status="${f.status}"> <b>${esc(f.fact_type)}</b><pre style="white-space:pre-wrap;margin:8px 0 0;color:var(--muted)">${esc(JSON.stringify(f.payload,null,2))}</pre><span class="tag">${esc(f.status)}</span></label>`).join('')||'<span class="tag">No extractable structured facts were found.</span>'}</div><div class="form-actions"><button class="btn secondary" onclick="closeModal()">Close</button><button class="btn danger" id="rejectFacts">Reject pending</button><button class="btn primary" id="applyFacts">Accept pending & update database</button></div>`);$('#applyFacts').onclick=async()=>{try{const r=await api('/api/documents/'+id+'/apply',{method:'POST',body:JSON.stringify({})});toast(`${r.applied} facts applied. Projects and schedule updated.`);closeModal();files()}catch(e){toast(e.message)}};$('#rejectFacts').onclick=async()=>{const ids=[...document.querySelectorAll('.factCheck[data-status="pending"]')].map(x=>x.value);try{await api('/api/documents/'+id+'/reject',{method:'POST',body:JSON.stringify({fact_ids:ids})});toast('Pending facts rejected');closeModal();files()}catch(e){toast(e.message)}}}catch(e){toast(e.message)}}

async function users(){const d=await api('/api/users');$('#content').innerHTML=`<div class="section-head"><div><h2>Users & Access</h2><p>Account directory. Passwords are never returned by this API.</p></div></div><div class="card table-wrap"><table class="table"><thead><tr><th>Name</th><th>Username</th><th>Email</th><th>Role</th><th>Company</th><th>Joined</th></tr></thead><tbody>${d.users.map(x=>`<tr><td><b>${esc(x.full_name)}</b></td><td>${esc(x.username)}</td><td>${esc(x.email)}</td><td>${esc(x.role)}</td><td>${esc(x.company||'—')}</td><td>${new Date(x.created_at).toLocaleDateString()}</td></tr>`).join('')}</tbody></table></div>`}
async function settings(){const [d,p]=await Promise.all([api('/api/auth/me'),api('/api/projects')]);$('#content').innerHTML=`<div class="grid page-grid"><div class="card"><div class="card-head"><h3>Account settings</h3></div><div class="card-body"><form id="settingsForm"><div class="form-grid"><div class="field"><label>Full name</label><input name="full_name" value="${esc(d.user.full_name)}"></div><div class="field"><label>Phone</label><input name="phone" value="${esc(d.user.phone||'')}"></div><div class="field"><label>Company</label><input name="company" value="${esc(d.user.company||'')}"></div><div class="field"><label>Department</label><input name="department" value="${esc(d.user.department||'')}"></div></div><div class="field"><label>Language</label><select name="preferred_language"><option value="en" ${state.lang==='en'?'selected':''}>English</option><option value="hi" ${state.lang==='hi'?'selected':''}>हिन्दी</option></select></div><div class="form-actions"><button class="btn primary">Save settings</button></div></form></div></div><div class="card"><div class="card-head"><h3>Security</h3></div><div class="card-body"><form id="passForm"><div class="field"><label>Current password</label><input type="password" name="currentPassword" required></div><div class="field"><label>New password</label><input type="password" name="newPassword" required></div><button class="btn secondary">Change password</button></form><hr style="border:0;border-top:1px solid var(--line);margin:25px 0"><button class="btn danger" onclick="logout()">Sign out</button></div></div><div class="card full"><div class="card-head"><div><h3>Test workspace data</h3><p style="color:var(--muted);margin:4px 0">Creates sample projects only inside your own account. It does not create or expose a demo login.</p></div><button class="btn secondary" ${p.projects.length?'disabled':''} onclick="importTestData()">Import sample project data</button></div></div></div>`;$('#settingsForm').onsubmit=async e=>{e.preventDefault();const b=Object.fromEntries(new FormData(e.currentTarget).entries());try{const r=await api('/api/auth/me',{method:'PATCH',body:JSON.stringify(b)});user=r.user;state.lang=r.user.preferred_language;localStorage.setItem('infra_lang',state.lang);toast('Settings saved');renderShell()}catch(e){toast(e.message)}};$('#passForm').onsubmit=async e=>{e.preventDefault();try{await api('/api/auth/change-password',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget).entries()))});e.currentTarget.reset();toast('Password changed')}catch(e){toast(e.message)}}}
function empty(title,msg){return `<div class="card" style="padding:40px;text-align:center;grid-column:1/-1"><h3>${title}</h3><p style="color:var(--muted)">${msg}</p></div>`}
function openProfile(){openModal('My profile',`<div class="grid page-grid"><div class="card" style="padding:20px"><div class="avatar" style="margin-bottom:15px">${esc((user.full_name||'PM').slice(0,2).toUpperCase())}</div><h3>${esc(user.full_name)}</h3><p style="color:var(--muted)">@${esc(user.username)}</p></div><div class="card" style="padding:20px"><p><b>Email</b><br>${esc(user.email)}</p><p><b>Role</b><br>${esc(user.role)}</p><p><b>Company</b><br>${esc(user.company||'Not set')}</p><p><b>Department</b><br>${esc(user.department||'Not set')}</p></div></div><div class="form-actions"><button class="btn primary" onclick="closeModal();navigate('settings')">Edit profile</button></div>`)}
function openModal(title,body){$('#modalRoot').innerHTML=`<div class="modal" onclick="if(event.target===this)closeModal()"><div class="modal-card"><div class="modal-head"><h3 style="margin:0">${title}</h3><button class="icon-btn" onclick="closeModal()">×</button></div><div class="modal-body">${body}</div></div></div>`}function closeModal(){$('#modalRoot').innerHTML=''}
function openProjectForm(id){let p={name:'',client:'',location:'',state:'',start_date:'',end_date:'',planned_progress:0,actual_progress:0,status:'In Progress'};if(id){const cards=state.data?.projects||[];p=cards.find(x=>x.id===id)||p}openModal(id?'Edit project':'Add project',`<form id="projectForm"><div class="form-grid"><div class="field"><label>Project name *</label><input name="name" required value="${esc(p.name)}"></div><div class="field"><label>Client</label><input name="client" value="${esc(p.client||'')}"></div><div class="field"><label>Location</label><input id="projectLocation" name="location" value="${esc(p.location||'')}"></div><div class="field"><label>State</label><input name="state" value="${esc(p.state||'')}"></div><div class="field"><label>Start date</label><input type="date" name="start_date" value="${esc(p.start_date||'')}"></div><div class="field"><label>End date</label><input type="date" name="end_date" value="${esc(p.end_date||'')}"></div><div class="field"><label>Planned progress %</label><input type="number" min="0" max="100" name="planned_progress" value="${p.planned_progress}"></div><div class="field"><label>Actual progress %</label><input type="number" min="0" max="100" name="actual_progress" value="${p.actual_progress}"></div><div class="field"><label>Status</label><select name="status"><option ${p.status==='In Progress'?'selected':''}>In Progress</option><option ${p.status==='Completed'?'selected':''}>Completed</option><option ${p.status==='Delayed'?'selected':''}>Delayed</option></select></div></div><div class="form-actions"><button type="button" class="btn secondary" onclick="closeModal()">Cancel</button><button class="btn primary">Save to database</button></div></form>`);$('#projectForm').onsubmit=async e=>{e.preventDefault();const b=Object.fromEntries(new FormData(e.currentTarget).entries());b.planned_progress=Number(b.planned_progress);b.actual_progress=Number(b.actual_progress);try{await api(id?'/api/projects/'+id:'/api/projects',{method:id?'PUT':'POST',body:JSON.stringify(b)});closeModal();projects();toast('Project saved to SQLite database')}catch(e){toast(e.message)}}}
async function removeProject(id){if(!confirm('Remove this project and its dependent activities?'))return;try{await api('/api/projects/'+id,{method:'DELETE'});projects();toast('Project removed from database')}catch(e){toast(e.message)}}
async function viewProjectActivities(id){navigate('schedule');setTimeout(()=>schedule(id),0)}
async function openActivityForm(a={}){const ps=(await api('/api/projects')).projects;if(!ps.length)return toast('Create a project first.');openModal(a.id?'Edit activity':'Add activity',`<form id="activityForm"><div class="form-grid"><div class="field"><label>Project *</label><select name="project_id" required>${ps.map(p=>`<option value="${p.id}" ${a.project_id===p.id?'selected':''}>${esc(p.name)}</option>`).join('')}</select></div><div class="field"><label>Activity code *</label><input name="code" required value="${esc(a.code||'')}"></div><div class="field"><label>Activity name *</label><input name="name" required value="${esc(a.name||'')}"></div><div class="field"><label>Status</label><select name="status"><option ${a.status==='In Progress'||!a.status?'selected':''}>In Progress</option><option ${a.status==='Completed'?'selected':''}>Completed</option><option ${a.status==='Delayed'?'selected':''}>Delayed</option></select></div><div class="field"><label>Planned progress %</label><input type="number" name="planned_progress" min="0" max="100" value="${a.planned_progress||0}"></div><div class="field"><label>Actual progress %</label><input type="number" name="actual_progress" min="0" max="100" value="${a.actual_progress||0}"></div><div class="field"><label>Planned start</label><input type="date" name="planned_start" value="${a.planned_start||''}"></div><div class="field"><label>Planned end</label><input type="date" name="planned_end" value="${a.planned_end||''}"></div></div><div class="form-actions"><button type="button" class="btn secondary" onclick="closeModal()">Cancel</button><button class="btn primary">Save to database</button></div></form>`);$('#activityForm').onsubmit=async e=>{e.preventDefault();const b=Object.fromEntries(new FormData(e.currentTarget).entries());b.planned_progress=Number(b.planned_progress);b.actual_progress=Number(b.actual_progress);try{await api(a.id?'/api/activities/'+a.id:'/api/activities',{method:a.id?'PUT':'POST',body:JSON.stringify(b)});closeModal();schedule();toast('Activity saved')}catch(e){toast(e.message)}}}
async function removeActivity(id){if(!confirm('Delete this activity?'))return;try{await api('/api/activities/'+id,{method:'DELETE'});schedule();toast('Activity deleted')}catch(e){toast(e.message)}}
async function openReportForm(){const p=(await api('/api/projects')).projects;let activities=[];for(const x of p){const a=await api('/api/projects/'+x.id+'/activities');activities.push(...a.activities.map(v=>({...v,project_name:x.name})))}openModal('Add daily progress report',`<form id="reportForm"><div class="form-grid"><div class="field"><label>Project</label><select id="reportProject" name="project_id"><option value="">Select project</option>${p.map(x=>`<option value="${x.id}">${esc(x.name)}</option>`).join('')}</select></div><div class="field"><label>Activity</label><select id="reportActivity" name="activity_id"><option value="">Select activity</option>${activities.map(a=>`<option value="${a.id}" data-project="${a.project_id}">${esc(a.code)} · ${esc(a.name)}</option>`).join('')}</select></div><div class="field"><label>Report date</label><input type="date" name="report_date" value="${new Date().toISOString().slice(0,10)}"></div><div class="field" style="position:relative"><label>Location (city/state)</label><input id="locationInput" name="location_text" autocomplete="off"><div id="citySuggestions" class="suggestions hidden"></div></div><div class="field"><label>Quantity</label><input type="number" step="0.01" name="quantity"></div><div class="field"><label>Unit</label><input name="unit" placeholder="m / % / units"></div></div><div class="field"><label>Description</label><textarea name="description" placeholder="Describe site progress, observations, constraints..."></textarea></div><div style="display:flex;gap:10px;align-items:center;margin:10px 0"><button type="button" class="btn secondary" id="gpsBtn">Use my current location</button><span id="gpsText" class="tag">No GPS selected</span></div><div id="reportMap" class="map"></div><input type="hidden" id="lat" name="latitude"><input type="hidden" id="lng" name="longitude"><div class="form-actions"><button type="button" class="btn secondary" onclick="closeModal()">Cancel</button><button class="btn primary">Save report to database</button></div></form>`);const map=L.map('reportMap').setView([22.9734,72.585],5);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{attribution:'© OpenStreetMap contributors'}).addTo(map);let marker;function pin(lat,lng,label){$('#lat').value=lat;$('#lng').value=lng;if(marker)marker.remove();marker=L.marker([lat,lng]).addTo(map).bindPopup(label||'Selected location').openPopup();map.setView([lat,lng],13);$('#gpsText').textContent=`${Number(lat).toFixed(5)}, ${Number(lng).toFixed(5)}`}$('#gpsBtn').onclick=()=>navigator.geolocation?navigator.geolocation.getCurrentPosition(pos=>pin(pos.coords.latitude,pos.coords.longitude,'Current location'),()=>toast('Location permission denied')):toast('Geolocation unavailable');$('#locationInput').oninput=async e=>{const term=e.target.value.trim();if(term.length<1)return $('#citySuggestions').classList.add('hidden');const arr=await api('/api/cities?q='+encodeURIComponent(term));$('#citySuggestions').innerHTML=arr.map(x=>`<div data-city="${esc(x.city)}, ${esc(x.state)}">${esc(x.city)}, ${esc(x.state)}</div>`).join('');$('#citySuggestions').classList.remove('hidden');document.querySelectorAll('#citySuggestions div').forEach(el=>el.onclick=()=>{$('#locationInput').value=el.dataset.city;$('#citySuggestions').classList.add('hidden')})};$('#reportProject').onchange=()=>{const pid=$('#reportProject').value;document.querySelectorAll('#reportActivity option[data-project]').forEach(o=>o.hidden=pid&&o.dataset.project!==pid)};$('#reportForm').onsubmit=async e=>{e.preventDefault();const b=Object.fromEntries(new FormData(e.currentTarget).entries());b.quantity=b.quantity?Number(b.quantity):null;try{const d=await api('/api/reports',{method:'POST',body:JSON.stringify(b)});closeModal();reports();toast(d.match.score===null?'Report saved; no match score was invented.':`Report saved. Database match: ${d.match.score}%`)}catch(e){toast(e.message)}}}
async function resolveAlert(id,resolved){try{await api('/api/alerts/'+id,{method:'PATCH',body:JSON.stringify({resolved})});alerts()}catch(e){toast(e.message)}}
async function importTestData(){try{const d=await api('/api/demo/import-test-data',{method:'POST'});toast(d.message);dashboard()}catch(e){toast(e.message)}}
function toggleTheme(){const dark=!document.body.classList.contains('dark');document.body.classList.toggle('dark',dark);api('/api/auth/me',{method:'PATCH',body:JSON.stringify({theme:dark?'dark':'light'})}).catch(()=>{});}
async function searchGlobal(){const q=$('#globalSearch').value.trim();const box=$('#searchResults');if(q.length<2)return box.classList.add('hidden');try{const d=await api('/api/search?q='+encodeURIComponent(q));box.innerHTML=d.results.length?d.results.map(x=>`<div class="search-result"><b>${esc(x.type)}</b> · ${esc(x.name||x.code||x.original_name||x.title||'')}</div>`).join(''):'<div class="search-result">No matches in your account.</div>';box.classList.remove('hidden')}catch(e){box.innerHTML='<div class="search-result">Search unavailable</div>';box.classList.remove('hidden')}}
function logout(){token=null;user=null;localStorage.removeItem('infra_token');authView('landing')}
window.authView=authView;window.navigate=navigate;window.openProjectForm=openProjectForm;window.removeProject=removeProject;window.openActivityForm=openActivityForm;window.removeActivity=removeActivity;window.openReportForm=openReportForm;window.resolveAlert=resolveAlert;window.openModal=openModal;window.closeModal=closeModal;window.askAI=askAI;window.importTestData=importTestData;window.logout=logout;window.reviewDocument=reviewDocument;
boot();
